# Python & Django Cache
*.log
*.pot
*.pyc
__pycache__/
local_settings.py

# Environment variables (Secrets)
.env

# Database (SQLite is not used in production on Heroku)
db.sqlite3
db.sqlite3-journal

# Wagtail/Django Media Uploads
# (User-uploaded content should not be in Git)
media/

# COLLECTED Static Files
# (This is the folder Heroku generates. Do NOT check this in.)
staticfiles/

# Editor & OS files
.vscode/
.DS_Store
Thumbs.db

# Custom ignores from your previous file
# CAREFUL: Only ignore these if they do NOT contain assets your site needs to display.
# If your templates verify specific images in 'img/', you should remove 'img/' from this list.
sass/
# img/      <-- Commented out to be safe. If you have site assets here, un-ignore them!
# webfonts/ <-- Commented out to be safe.